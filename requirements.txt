fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
pydantic==2.10.0
python-dotenv

# Core dependencies - install first
numpy>=2.0.2  # Required by whisperx for Python 3.12
torch>=2.6.0  # Let whisperx determine compatible version
torchaudio>=2.6.0
--extra-index-url https://download.pytorch.org/whl/cu124

# Additional dependencies
ffmpeg-python
boto3
mysql-connector-python
requests

# LLM for analysis (optional but recommended)
transformers
accelerate
huggingface-hub

# PII Detection and Redaction
presidio-analyzer
presidio-anonymizer
soundfile
scipy
spacy>=3.7,<3.9  # Constrain spacy to versions with pre-built wheels

# WhisperX - install last after dependencies
git+https://github.com/m-bain/whisperx.git

# Optional for better diarization (requires HuggingFace token)
# pyannote.audio==3.1.0
